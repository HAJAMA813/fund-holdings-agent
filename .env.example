# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 排除，不会进入 git。
# 也可以直接在终端 export 这些变量（export 只对当前终端窗口有效）。

DEEPSEEK_API_KEY=sk-你的key
DEEPSEEK_MODEL=deepseek-chat
# 可选：自定义 API 基础地址，默认 https://api.deepseek.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com
